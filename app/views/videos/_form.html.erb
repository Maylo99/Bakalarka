<%= form_with model: video, class: "w-100 bg-dark text-white rounded pb-2" do |form| %>
  <% if video.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(video.errors.count, "error") %> prohibited this video from being saved:</h2>
      <ul>
        <% video.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

   <div class="form-row d-flex justify-content-center">
     <div class="col-lg-6">
       <%= form.label :Názov_videa, style: "display: block", class: "form-label" %>
       <%= form.text_field :title, class: "form-control" %>
     </div>
   </div>
   <div class="form-row  d-flex justify-content-center">
     <div class="col-lg-6">
       <%= form.label :Vyber_kurz, :Vyber_kurz, style: "display: block", class: "form-label" %>
       <%= form.collection_select(:course_id, Course.all, :id, :title, {}, { class: "form-control" }) %>
     </div>
   </div>
   <div class="form-row  d-flex justify-content-center">
     <div class="col-lg-6">
       <%= form.label :Popis, style: "display: block", class: "form-label" %>
       <%= form.text_area :description, class: "form-control" %>
     </div>
   </div>
   <div class="form-row  d-flex justify-content-center">
     <div class="col-lg-6">
       <%= form.label :Video, class: "form-label" %>
       <%= form.file_field :file, as: :file, class: "form-control" %>
     </div>
   </div>
   <div class="form-row  d-flex justify-content-center">
     <div class="col-lg-6">
       <%= form.submit "Pridaj video" %>
     </div>
   </div>
<% end %>
