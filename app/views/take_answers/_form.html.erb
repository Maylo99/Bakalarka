<%= form_with(model: take_answer) do |form| %>
  <% if take_answer.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(take_answer.errors.count, "error") %> prohibited this take_answer from being saved:</h2>

      <ul>
        <% take_answer.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :user_answer, style: "display: block" %>
    <%= form.text_area :user_answer %>
  </div>

  <div>
    <%= form.label :take_test_id, style: "display: block" %>
    <%= form.text_field :take_test_id %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
